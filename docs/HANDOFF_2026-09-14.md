# 2026-09-14 交接：双线外景初稿与待修复问题

## 下一轮先处理这三个问题（用户实机反馈，均未修复）

1. **白菜和建筑／信箱比例不对。** 用户指出白菜比屋子小太多，甚至没有信箱大。当前 `scenes/cabbage_act2/exterior.gd` 三张外景共用 `visible_height = 105.0`，主屋原图1672×941，另外两张1448×1086，视口再按整图缩放；不能仅根据素材画布尺寸或单一数值判断角色比例。下一轮按实际可见身体高度、脚底、门洞与信箱校准各机位，必要时调整镜头和景物关系；不要只把角色放大后便宣称合格。验收：主屋门前、信箱旁、道路、蘑菇屋门前截图对比，正常速度来回走时比例与落地连续合理，由用户确认。
2. **小呆猪通往山洞的场景空间设定有问题，可以直接站在山洞上。** 用户称“小呆猪穿越走廊的山洞不对”。优先检查 `scenes/chapter2/forest_path.gd` 与 `assets/chapter2/forest_path_v1.png`（通往发光洞口的小路），同时核对 `scenes/chapter2/cave.gd` 的洞内入口衔接。当前小路以两块墙多边形限制通行，洞口交互点(1130,230)；洞口视觉、实体山体和角色站立平面需重新核对。具体越界位置与根因尚未复现，不把问题直接归结为某个坐标。应让角色只能走在地面／入口前，不能爬上洞顶或山壁；检查普通走路、1.5倍速度、跳跃和翻滚，必要时重构入口布局／背景／遮挡，而非只补一块挡路墙。
3. **白菜出门后无法选择进入右前方小路。** 当前 `exterior.gd` 主屋出口数据是(1260,835)，按 E 的距离阈值100；脚本监听 `interact`，只有场内文字提示，没有目的地选择菜单。用户报告实际无法选择进入小路；根因尚未复现，需检查从主屋出生点到触发区的真实可达性、脚底碰撞、坐标与视觉指向、输入消费及交互反馈，并参照屋内出口的选择体验决定入口交互方式。不要通过直接调用 `travel("path")` 证明可玩。验收必须用真实方向键从门前走到右前方，再用真实 E／选择操作进入小路，返回后也能再次进入。

上述反馈覆盖此前静态检查与逻辑测试的结论：本轮外景仍是未通过用户验收的初稿，不应描述为比例正确或全路线可玩。

## 必须保留的故事顺序

共同开场 → 白菜屋内调查、准备出门 → 切小呆猪森林线 → 打败史莱姆 → **拾取第一块礼物碎片后**切回小男孩白菜 → 主屋外景 → 右前方小路 → 呆呆猪蘑菇屋探索。

- 只打怪还不切线；拿碎片后才切。
- main villa 是两人主要住所，蘑菇屋是呆呆猪的小屋，在出门后的右前方。
- 白菜本来是小男孩；因忘记呆呆猪失去幸福连接，才从白菜恢复小男孩，不是被诅咒成白菜。
- 第二幕外景没有室内调查，当前只做到蘑菇屋门口。
- 白菜回主屋后再出门不重播小猪森林线。

## 今天已实现并纳入本次提交

- `assets/cabbage_act2/`：用户mainvilla原图、内置image_gen生成的白天蘑菇屋和花园小路；夜景原稿未覆盖。提示词见 `art_direction/cabbage_act2_prompts.md`。
- `scenes/cabbage_act2/`：三幅独立机位、外景路由、道路边界、E提示、F5保存、Esc菜单、返回客厅。路线交互有上述用户反馈，待修复。
- `chapter1/chapter_room.gd`、`chapter2/cave.gd`：双线交叉衔接、碎片拾取后存档目的地为白菜外景、已完成旧山洞存档自动切回白菜。
- `actors/shared/player_controller.gd`：普通步行统一1.5倍，白菜屋内255、外景232.5、小猪307.5像素/秒。剧情walk_to也沿用此倍率；翻滚和全局时间不加速。步态按实际位移推进。
- 配套测试与方向／试玩文档。今天较早的中文字体及战斗改动已经在 `c918dc5`，本次不重复制作。

## 验证证据与边界

- 外景初稿：CABBAGE_EXTERIOR_OK、CHAPTER1_TEST_OK、CHAPTER1_EXIT_ROUTING_OK，三场景Apple M1 OpenGL静态截图检查。
- 双线修正：CHAPTER1_EXIT_ROUTING_OK、CHAPTER2_FLOW_OK、CHAPTER2_PAUSE_MENU_OK，包含拾碎片后切白菜、存档、回屋再出门及旧山洞状态。
- 加速之后：GAMEPLAY_TEST_OK、CABBAGE_EXTERIOR_OK。完整森林流程未在1.5倍速度下重跑。
- 外景测试直接调用travel验证路由，仅测试一处主屋边界；未覆盖真实E进入右前方小路，也未证明所有道路连续可达。森林逻辑测试也不证明洞口视觉空间合理。
- 原有三姿势男孩、程序翻滚／招架／攻击、临时音效继续使用；无新增正式步行动画、室外环境音、开门动作。未完成人工正常速度连续动画验收。
- 背景单层、4:3画面宽屏留黑、道路再绘建筑细节与参考存在差异。源资源与生成提示词保留。
- 测试关闭存档或用独立临时路径；headless有已知系统证书与日志目录权限提示。

## 接手方式

先读 AGENTS.md、WORKLOG.md 最近记录及 STORY_AND_ART_DIRECTION.md。打开 `scenes/cabbage_act2/villa.tscn` 按F6（Mac可能fn+F6）复现比例和入口；打开 `scenes/chapter2/forest_path.tscn` 复现山洞通行。先确认三处问题并做短段正常速度验证，再扩展蘑菇屋室内。

本轮用户要求先写交接并commit/push，因此三个反馈只记录，不在本次扩大为修复任务。提交与推送结果以Git记录及本轮最终回复为准。
