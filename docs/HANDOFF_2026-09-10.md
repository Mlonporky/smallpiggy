# 2026-09-10 交接：第一幕探索收尾

> 历史交接：第二幕现已实现入口与山洞战斗，下文“第二幕尚未实现”的状态已被 [第二幕交接](HANDOFF_2026-09-10_ACT2.md) 取代。第一幕细节仍可参考。

下一步依次做 **厨房冰箱探索**、**第二幕小呆猪黑暗森林**。这两项尚未在本轮实现。本文记录截至本次提交的状态；历史 WORKLOG 中被后续修改取代的数值、门位和动画不再作为当前基准。

## 当前可玩状态

- 现行第一幕入口：`scenes/chapter1/bedroom.tscn`，厨房／客厅同目录。`chapter_01_hollow_heart` 是历史样板。
- 三张高清背景已接入 `assets/chapter1/*_v2.png`。连接为卧室右门 → 厨房；厨房左门 → 卧室、右门 → 客厅；客厅左门 → 厨房、底部格子 → 出门。宽门前区域自动显示目的地确认；取消后原地不重复弹窗，离开再进入可重触发。保留剧情锁和返回落点。
- 猪杯、两套餐具、礼物碎片使用高清图；餐具调查左右并排展示。白菜杯地图局部已修正。抱枕近景改为柔软绒布、缝线和刺绣质感，参考原图保留。背景画里的餐盘和沙发抱枕没有另行重画。
- 第一幕支持靠近物品后点击调查，E 不要求朝向；杯子柜台前、餐桌周围采用宽可达范围，仍保留距离、咖啡前置和剧情忙碌锁。旧森林交互不受这项修改影响。
- 客厅茶几与沙发的碰撞间隙扩大至 60px，小男孩可以双向通过，家具仍阻挡。
- 垃圾桶只显示“里面没什么值得留念的东西。”，无放大图，不推进回忆。桶上沿有柔和呼吸星光，读完后记录 `trash_checked` 并收起标记。
- 探索环境暗幕为 0.50，未调查／已调查亮区强度为 0.38／0.18。保留小星光，不恢复强烈明暗反差。
- 苏醒对白顺序：“好安静” → “胸口……怎么空空的” → “屋子里怎么这么昏暗” → “以前也这么安静吗？”

## 用户已保存的三次回忆

`MemoryAnchor` 的 Position、Scale、Modulate 是运行时权威来源；不要用脚本中的旧坐标覆盖。编辑方法见 [手动调整回忆影子](MEMORY_POSITION_EDIT_ZH.md)。

| 场景 | Position | Scale | Modulate RGBA |
| --- | --- | --- | --- |
| 卧室 | (525, 740) | (0.17831668, 0.17831668) | (0.99, 0.90, 0.93, 0.42) |
| 厨房 | (946.9872, 706) | (0.12836517, 0.13409415) | (0.99, 0.85, 0.89, 0.55) |
| 客厅 | (713.98724, 916) | (0.18362355, 0.15406564) | (0.98, 0.80, 0.86, 0.68) |

三次均固定位置，仅淡入、淡出；没有上浮、镜头推拉或闪白。第三次最清晰、颜色最深，第二次比第三次淡，第一次最淡。保留原模糊影子图，额外模糊为 0.005／0.004／0.003。编辑器背景和影子辅助节点运行时隐藏。

## 下一步一：冰箱探索

当前厨房冰箱只是背景，尚无冰箱调查热点、对白、近景或完成标记。

1. 先检查已有附件是否有冰箱内容，再向用户确认缺少的对白、是否放大／开门及是否影响主线。尚未确认的内容不能写成定稿对白。
2. 在 `room_layout.gd` 添加热点；`chapter_room.gd` 添加点击区域、靠近范围和交互分支。沿用附近即可调查的机制，避免要求站在单点或穿过冰箱碰撞。
3. 在 `exploration_light.gd` 添加与垃圾桶同等级的柔和标记；是否增加局部亮区按最终内容决定。
4. 若需要完成状态，沿用 GameState 标记与存档机制。检查重复调查、读档、咖啡／餐具流程及出口门槛不被意外改变。

## 下一步二：小呆猪黑暗森林

离家已经路由到 `scenes/chapter_02_dark_forest/forest_clearing.tscn`，这仍是旧玩法样板，不是完整第二幕。

可复用现状：`forest_clearing.gd` 已有开场对白、3 点生命与近战、史莱姆战斗、胜利后显现礼物碎片、拾取后保存、死亡重试、完成存档恢复。碎片 ID 是 `fragment_red_wrap_01`，完成标记是 `forest_slime_defeated`；重复收集幂等，不增加 Heart。对白在 `data/dialogue/chapter_02.json`。

推进顺序：盘点已有森林、小猪、史莱姆和碎片素材与剧本 → 确认进入森林的分镜和探索／战斗节奏 → 先做短场景验证角色比例、手绘风格、移动与攻击 → 接入现有战斗和碎片存档 → 验证离家切猪、战斗死亡重试、拾取及读档。返回山庄、重逢和结局尚未实现，不应误报为已有完整流程。

故事基线：小呆猪是女生，始终记得白白菜；她找礼物碎片，没有失忆恢复系统。白菜原本是小男孩，感到幸福才变白菜；诅咒使他变回小男孩。最终没有 Boss 战，巫师死于白菜认出小猪时爆发的快乐能量。后续遵循 [方向文档](STORY_AND_ART_DIRECTION.md) 与仓库 AGENTS.md。

## 代码入口与验证

| 文件 | 职责 |
| --- | --- |
| `scenes/chapter1/room_layout.gd` | 背景、碰撞、热点、出生点及出口区域 |
| `scenes/chapter1/chapter_room.gd` | 调查、剧情门槛、回忆、点击和距离判断 |
| `scenes/chapter1/room_exits.gd` | 接近房门、确认、取消及重新触发 |
| `scenes/chapter1/chapter_presentation.gd` | 对白、高清近景、双图与目的地菜单 |
| `scenes/chapter1/exploration_light.gd` / `.gdshader` | 柔和环境光和物品标记 |
| `data/dialogue/chapter1.json` | 当前第一幕对白 |

使用 Godot 4.7.2。测试命令示例：`godot --headless --path . --script tests/chapter1_polish_test.gd`。本机二进制位于 `/private/tmp/godot-4.7.2-check/Godot.app/Contents/MacOS/Godot`，属于临时工具路径，不是项目依赖。

本轮功能测试覆盖：chapter1_test、chapter1_navigation_test、chapter1_ui_test、chapter1_exit_routing_test、chapter1_bottom_exit_test、chapter1_prop_click_test、chapter1_polish_test、memory_anchor_test、boy_atlas_test、gameplay_test。`bottom_exit` 测试名保留，但现行内容验证五处门前区域，不代表恢复全房间底部菜单方案。最终复测结果记录在 WORKLOG。

已进行实机静态截图检查：三房间探索光、三次渐进影子、高清道具近景和垃圾桶星光。可用 `chapter1_props_visual_check.gd`、`exploration_light_visual_check.gd` 重现；临时截图不随仓库发布。逻辑与截图通过不等于完整动态动画／美术验收。

剩余制作限制：角色仍为既有三姿势素材，起床仍为近景；未补完整手绘角色动作或音频。背景外缘仍保留卧室黑色、厨房／客厅白色差异。FG 与 BG 未完全配准，家具局部遮挡继续取 BG 同坐标像素。男孩动画表必须保留行边界 `[0,368,694,1028,1448]`，不能改回高度四等分。
